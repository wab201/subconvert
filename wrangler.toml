# 重命名为 wrangler.toml 后生效；name 不影响 Git 导入时的项目名（以 Dashboard 填写为准）。
# KV 变量名 SUBCONVERT_KV 必须固定，把下方 id 换成你的 KV 命名空间 ID 即可。

name = "subconvert"
pages_build_output_dir = "public"
compatibility_date = "2024-11-01"

[[kv_namespaces]]
binding = "dy"
id = "19cba73d8a6c4d978a91847e06cd19ad"
